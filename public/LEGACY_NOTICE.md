# Legacy static app

The files in this directory are an older static app artifact.

Do not use this directory as the production deployment target for the Employee Hub.

The intended app is the Next.js application in `app_v2/`.

The live login issue seen on the old deployment belongs to this legacy bundle, not to the current `app_v2` application.
